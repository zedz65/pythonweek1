def tue(word,phrase):
    word2 = word.lower()
    newlist = phrase.split(":")
    emptylist = []

    for i in range (len(newlist)):
    newlist[i] = newlist[i].lower()
    if newlist[i] = newlist[i].lower()
    emptylist.append(newlist)):
    
    result = ":".join(emptylist)
return result